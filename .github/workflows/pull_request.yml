name: Pull Request CI

on: pull_request

jobs:
  prettier:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v5
      - name: Set up Node.js
        uses: actions/setup-node@v6
        with:
          node-version: '22'
      - name: Install dependencies
        run: npm ci
        env:
          FAST_FOLDER_SIZE_DU_ZIP_LOCATION: "DU.zip"
      - name: Run prettier
        run: npm run format:check

  eslint:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v5
      - name: Set up Node.js
        uses: actions/setup-node@v6
        with:
          node-version: '22'
      - name: Install dependencies
        run: npm ci
        env:
          FAST_FOLDER_SIZE_DU_ZIP_LOCATION: "DU.zip"
      - name: Run eslint
        run: npm run lint

  # TODO: Add unit tests
  # unit-tests:
  #   runs-on: ubuntu-latest
  #   steps:
  #     - name: Checkout repository
  #       uses: actions/checkout@v5
  #     - name: Set up Node.js
  #       uses: actions/setup-node@v6
  #       with:
  #         node-version: '22'
  #     - name: Install dependencies
  #       run: npm ci
  #       env:
  #         FAST_FOLDER_SIZE_DU_ZIP_LOCATION: "DU.zip"
  #     - name: Run unit tests
  #       run: npm run test:unit

  api-tests:
    needs: [prettier, eslint]
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v5
      - name: Install dependencies
        run: npm ci
        env:
          FAST_FOLDER_SIZE_DU_ZIP_LOCATION: "DU.zip"
      - name: Execute API tests
        run: npm run test:api
